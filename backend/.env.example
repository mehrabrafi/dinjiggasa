# Database
DATABASE_URL="postgresql://user:password@localhost:5432/dinjiggasa?schema=public"

# Server
PORT=3001

# JWT - REQUIRED: Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Wasabi S3 Storage
WASABI_ACCESS_KEY_ID=
WASABI_SECRET_ACCESS_KEY=
WASABI_BUCKET_NAME=
WASABI_REGION=
WASABI_ENDPOINT=
WASABI_PUBLIC_URL=

# ZeptoMail
ZEPTOMAIL_URL=https://api.zeptomail.com/v1.1/email
ZEPTOMAIL_TOKEN=
ZEPTOMAIL_FROM_ADDRESS=noreply@yourdomain.com
ZEPTOMAIL_FROM_NAME=DinJiggasa

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:3000
