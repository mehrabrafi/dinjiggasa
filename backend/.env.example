# Database
DATABASE_URL="postgresql://user:password@localhost:5432/dinjiggasa?schema=public"

# Server
PORT=3001

# JWT - REQUIRED: Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Wasabi S3 Storage
WASABI_ACCESS_KEY_ID=
WASABI_SECRET_ACCESS_KEY=
WASABI_BUCKET_NAME=
WASABI_REGION=
WASABI_ENDPOINT=
WASABI_PUBLIC_URL=

# Sender.net
SENDER_API_TOKEN=
SENDER_FROM_ADDRESS=contact@deenjiggasa.info
SENDER_FROM_NAME=DinJiggasa

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:3000
