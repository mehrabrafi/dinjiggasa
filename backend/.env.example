# Database
DATABASE_URL="postgresql://user:password@localhost:5432/dinjiggasa?schema=public"

# Server
PORT=3001

# JWT - REQUIRED: Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Cloudflare R2 Storage
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT=
R2_PUBLIC_URL=

# Zoho ZeptoMail Settings
ZEPTOMAIL_API_TOKEN=your_zeptomail_api_token
ZEPTOMAIL_SENDER_ADDRESS=contact@yourdomain.com
ZEPTOMAIL_SENDER_NAME=DinJiggasa

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:3000
